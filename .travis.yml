language: python
dist: trusty
sudo: required

python:
  - "3.4"
  - "3.5"
  - "nightly"

install:
  - pip3 install pyqt5
  - pip3 install numpy

script:
  - python3 setup.py build_ext
  - cd ./tests
  - export PYTHONPATH="../src"
  - py.test -v